define('word-export:controllers/word-template', ['controllers/record'], function (Dep) {

    return Dep.extend({

        // Default controller for WordTemplate entity
        // Inherits all functionality from the base record controller

    });
});
